# The backend image builds from the repo root (docker build -f backend/Dockerfile .)
# so skill/ can ship in the image. This file also governs the deploy dir on the
# build node, where .env (secrets) and any database dump must never enter a
# build context.
.git
.env
backups
*.sql
ios
planning
deploy
mcp
docker-stack.yml
backend/tests
**/__pycache__
**/.venv
**/data
**/htmlcov
**/.pytest_cache
**/.ruff_cache
**/.coverage
